Assumptions:
A, B, C, D: Point

triangle(A, B, C)
between(B, D, C)

Line(A, D) == internal_angle_bisector(B, A, C)

Need to prove:
distance(A, B) * distance(C, D) == distance(A, C) * distance(B, D)

Proof:
