/// The maximum number of arguments in a predicate or a construction.
/// (Used to improve of the `SmallVec` type when holding arguments for predicates and constructions)
pub(crate) const MAX_ARGS: usize = 4;
